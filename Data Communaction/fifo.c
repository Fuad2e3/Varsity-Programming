#include <stdio.h>
int main (){
    int np;

    printf("Enter number of pages:");
    scanf("%d", &np);

    printf("Enter of pages:");
    for(i=o; i<np; i++){
            scanf("%d", &pages[i]);
    }

}